import 'package:flutter/material.dart';

class PatientRecordScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Patient Record'),
      ),
      body: Center(
        child: Text('Patient Record Content Here'),
      ),
    );
  }
}
