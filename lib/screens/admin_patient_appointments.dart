import 'package:flutter/material.dart';

class PatientAppointments extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Patient Appointment List'),
      ),
      body: Center(
        child: Text('Patient Appointment List Content Here'),
      ),
    );
  }
}
