import 'package:flutter/material.dart';

class ListOfPatients extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('List of Patients'),
      ),
      body: Center(
        child: Text('List of Patients Content Here'),
      ),
    );
  }
}
