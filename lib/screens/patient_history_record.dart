import 'package:flutter/material.dart';

class PatientHistory extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Patient History'),
      ),
      body: Center(
        child: Text('Patient History Content Here'),
      ),
    );
  }
}
