import 'package:flutter/material.dart';

class AppointmentScheduleScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Appointment Schedule'),
      ),
      body: Center(
        child: Text('Appointment Schedule Content Here'),
      ),
    );
  }
}
